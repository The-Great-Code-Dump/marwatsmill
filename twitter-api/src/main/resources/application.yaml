spring:
  application:
    name: "TwitterEnhancerApi"
  main:
    banner-mode: "off"
    allow-bean-definition-overriding: true
  datasource:
    url: "jdbc:mysql://localhost:3306/twitter-api-enhancer"
    username: "root"
    password: "root"

server:
  port: 8080
  shutdown: graceful